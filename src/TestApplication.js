/**
 * Application class
 * Store session data to be easily accessed between navigations
 */
export class TestApplication {
    /* eslint-disable-next-line no-empty-function */
    async init() {
    }
}
